import { useState } from 'react';
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { Building2, Plus, RefreshCw, Search, X } from 'lucide-react';
import { suppliers, type SupplierInput } from '../api';

type Lang = 'en' | 'ar';
const emptyForm: SupplierInput = { name: '', contactPerson: '', phone: '', email: '', address: '', notes: '' };

export default function Suppliers({ lang }: { lang: Lang }) {
  const isRtl = lang === 'ar';
  const qc = useQueryClient();
  const [search, setSearch] = useState('');
  const [form, setForm] = useState<SupplierInput>(emptyForm);
  const [showForm, setShowForm] = useState(false);
  const [error, setError] = useState('');
  const list = useQuery({ queryKey: ['desktop-suppliers', search], queryFn: async () => { const result = await suppliers.list({ search, limit: 100, isActive: true }); return Array.isArray(result) ? result : result.items || []; } });
  const create = useMutation({ mutationFn: () => suppliers.create(form), onSuccess: () => { setForm(emptyForm); setShowForm(false); setError(''); qc.invalidateQueries({ queryKey: ['desktop-suppliers'] }); }, onError: (err: any) => setError(err.message || (isRtl ? 'تعذر إنشاء المورد' : 'Could not create supplier')) });
  const setField = (key: keyof SupplierInput, value: string) => setForm(current => ({ ...current, [key]: value }));
  const supplierRows = list.data || [];
  return <section className="desktop-page" dir={isRtl ? 'rtl' : 'ltr'}><div className="page-heading"><div><span className="eyebrow">{isRtl ? 'المشتريات' : 'Procurement'}</span><h1>{isRtl ? 'الموردون' : 'Suppliers'}</h1><p>{isRtl ? 'إدارة جهات التوريد المرتبطة بالمشتريات.' : 'Manage suppliers connected to purchase orders.'}</p></div><div className="report-controls"><button className="secondary-action" onClick={() => list.refetch()}><RefreshCw size={16} /></button><button className="primary-action" onClick={() => setShowForm(true)}><Plus size={16} /> {isRtl ? 'مورد جديد' : 'New supplier'}</button></div></div><div className="toolbar"><div className="search-box"><Search size={17} /><input value={search} onChange={event => setSearch(event.target.value)} placeholder={isRtl ? 'ابحث بالاسم أو الهاتف...' : 'Search name or phone...'} /></div></div>{list.isLoading && <div className="state-panel">{isRtl ? 'جاري تحميل الموردين...' : 'Loading suppliers...'}</div>}{list.isError && <div className="state-panel">{isRtl ? 'تعذر تحميل الموردين.' : 'Could not load suppliers.'}</div>}{!list.isLoading && !list.isError && supplierRows.length === 0 && <div className="state-panel">{isRtl ? 'لا توجد نتائج.' : 'No suppliers found.'}</div>}{!list.isLoading && !list.isError && supplierRows.length > 0 && <div className="supplier-list">{supplierRows.map(supplier => <article className="supplier-row" key={supplier.id}><div className="customer-avatar"><Building2 size={18} /></div><div className="customer-main"><strong>{supplier.name}</strong><span>{supplier.contactPerson || supplier.phone || supplier.email || (isRtl ? 'لا توجد بيانات اتصال' : 'No contact details')}</span></div><span className={supplier.isActive ? 'status-pill' : 'status-text'}>{supplier.isActive ? (isRtl ? 'نشط' : 'Active') : (isRtl ? 'غير نشط' : 'Inactive')}</span></article>)}</div>}{showForm && <div className="modal-backdrop"><div className="payment-modal supplier-form"><button className="drawer-close" onClick={() => setShowForm(false)}><X size={18} /></button><h2><Building2 size={20} /> {isRtl ? 'إنشاء مورد' : 'Create supplier'}</h2>{error && <div className="inline-error">{error}</div>}<label>{isRtl ? 'اسم المورد' : 'Supplier name'}<input value={form.name} onChange={event => setField('name', event.target.value)} required /></label><label>{isRtl ? 'جهة الاتصال' : 'Contact person'}<input value={form.contactPerson} onChange={event => setField('contactPerson', event.target.value)} /></label><label>{isRtl ? 'الهاتف' : 'Phone'}<input value={form.phone} onChange={event => setField('phone', event.target.value)} /></label><label>{isRtl ? 'البريد الإلكتروني' : 'Email'}<input type="email" value={form.email} onChange={event => setField('email', event.target.value)} /></label><label>{isRtl ? 'العنوان' : 'Address'}<input value={form.address} onChange={event => setField('address', event.target.value)} /></label><div className="modal-actions"><button className="secondary-action" onClick={() => setShowForm(false)}>{isRtl ? 'إلغاء' : 'Cancel'}</button><button className="primary-action" disabled={create.isPending || !form.name.trim()} onClick={() => create.mutate()}>{create.isPending ? (isRtl ? 'جاري الحفظ...' : 'Saving...') : (isRtl ? 'حفظ المورد' : 'Save supplier')}</button></div></div></div>}</section>;
}
